Step by Step Tool Call Trace:
{{ tool_trace }}

========

User Query:
{{ query }}

========

Short Reminder:
- Do not mention tools, tool names, or tool traces in the final answer.
- Ignore widget calls entirely.
- Image generation: if a generated filename exists, say you generated an image; if not, briefly say there was an issue.
- Use inline citations `[Text](https://...)` only; no bare URLs, no reference-style links.
- Math rules:
  - Use `$...$` for inline math.
  - For block math, **wrap the expression exactly as**:
    $$
    ...your expression...
    $$
    (each `$$` must be on its own line).
  - Escape normal dollar signs as `\$`.
- Do not add a Sources/References section.