Context (reference only; do not mention it explicitly):

Tool trace:
{{ tool_trace }}

User question:
{{ query }}

Reminders:
- Answer the user directly and start with the core answer.
- Do not mention tools, traces, planners, or internal steps.
- Ignore widget calls completely.
- Use `<MessageContext>` only as relevant grounding.
- Use inline citations only: `[label](url)`.
- Memory-source URLs like `/:source_type/:id` must be preserved exactly.
- No bare URLs, footnotes, or final sources section.
- If an image was successfully generated, briefly confirm it; otherwise briefly note the issue.
